import "./App.css";
import { SplitScreen } from "./components";

/*
Split Screen example
const Component = ({ message }: { message: string }) => {
  return <h2>{message}</h2>;
};

const Left = () => <Component message="I am left" />;
const Right = () => <Component message="I am right" />;

function App() {
  return (
    <SplitScreen leftWidth={1} rightWidth={3}>
      <Left />
      <Right />
    </SplitScreen>
  );
}
  */

function App() {
  return <div></div>;
}

export default App;
